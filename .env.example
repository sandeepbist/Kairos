# Kairos Production Environment Configuration Template

# Core
APP_NAME="Kairos Ambient Action Agent"
APP_ENV=development
DEBUG=true
SANDBOX_MODE=false

# Database & Temporal
POSTGRES_USER=kairos_user
POSTGRES_PASSWORD=kairos_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5435
POSTGRES_DB=kairos_db
REDIS_HOST=localhost
REDIS_PORT=6381
TEMPORAL_HOST=localhost:7234

# Security Vault Key (Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=eWF5c29tZXJhbmRvbWtleWZvcmRldmVsb3BtZW50MTIzNDU=

# AI Extraction Foundation Models (Optional: Can also be saved directly via Settings UI)
GOOGLE_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
DEFAULT_MODEL_NAME=gemini-2.5-flash

# Target Tool MCP Connectors (Optional: Can also be saved directly via Settings UI)
NOTION_API_KEY=
JIRA_API_TOKEN=
JIRA_EMAIL=
JIRA_DOMAIN=
GOOGLE_CALENDAR_ACCESS_TOKEN=
