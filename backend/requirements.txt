# Core API & Async
fastapi>=0.141.1
uvicorn[standard]>=0.53.0
pydantic>=2.13.5
pydantic-settings>=2.15.0
httpx>=0.28.1
asyncpg>=0.31.0
sqlalchemy[asyncio]>=2.0.54
cryptography>=50.0.1

# AI, Agentic & Memory (semantic routing memory is implemented natively)
langgraph>=1.2.12
langchain-core>=1.6.4
langchain-google-genai>=4.4.0
langchain-openai>=1.6.3

# Model Context Protocol (MCP)
mcp>=2.2.0

# Slack Socket Mode (ingestion bot; builtin websocket client, no extra deps)
slack-sdk>=3.44.1

# Standard Webhooks official verifier — the TEST ORACLE for our signer
# (the runtime sender is stdlib hmac; this lib never runs in prod paths)
standardwebhooks>=1.1.0

# Durable Execution
temporalio>=1.33.0

# Migrations
alembic>=1.20.0

# Observability
langfuse>=4.15.4

# Testing
pytest>=9.1.1
pytest-asyncio>=1.4.0
